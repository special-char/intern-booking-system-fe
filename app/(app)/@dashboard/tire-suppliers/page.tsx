import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Treadcommand | Tire Suppliers",
  description: "Treadcommand | Tire Suppliers",
};

export default function TireSuppliersPage() {
  return (<></>);
}
